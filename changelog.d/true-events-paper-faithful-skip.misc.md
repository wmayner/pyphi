Replaced the ``test_true_events`` pin with a paper-citation skip plus a sibling regression test ``test_true_events_on_known_complex`` that pins ``indices=(1, 2)`` explicitly. Major-complex selection at ``current_state=(0,0,1)`` on the IIT 3.0 standard substrate is genuinely tied between SIAs over ``(0, 2)`` and ``(1, 2)`` (both at φ=1.0); the IIT 3.0 paper (Albantakis et al. 2019, p.17) explicitly says symmetric ties of this form are "undetermined" — neither winner is paper-canonical. The downstream regression target (true_events output shape on a known irreducible complex) is now covered by the sibling test, decoupled from any tie-break policy. ``test_true_events`` stays skipped until pyphi adopts an explicit policy for symmetric major-complex ties.
