The ``presets.iit3`` preset matches ``pyphi_config_3.0.yml`` exhaustively: ``purview_tie_resolution`` is ``["PHI", "PURVIEW_SIZE"]``, ``assume_partitions_cannot_create_new_concepts`` is ``False``, and ``actual_causation.alpha_measure`` is ``"PMI"`` — all set explicitly rather than relying on the inherited defaults to happen to match. ``IITConfig.purview_tie_resolution`` is annotated ``str | list[str]`` to match the underlying :func:`pyphi.resolve_ties.purviews` contract. Substrates with phi-ties at the purview level (``rule110`` at state ``(1, 0, 1)``, ``grid3`` at state ``(1, 0, 0)``) resolve via the secondary ``PURVIEW_SIZE`` key under the preset.
