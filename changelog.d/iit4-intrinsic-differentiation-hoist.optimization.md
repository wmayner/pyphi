The IIT 4.0 system-level SIA no longer recomputes ``intrinsic_differentiation`` for every candidate system partition. The quantity depends only on ``(direction, system)``, not the partition, so it is now computed once per direction by the MIP search and threaded into each partition's evaluation. Φ, the MIP, and the ``intrinsic_differentiation`` carried on the result are unchanged.
