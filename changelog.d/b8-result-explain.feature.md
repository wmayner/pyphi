Added `result.explain()` — a typed, displayable account of why a Φ/φ/α
quantity came out as it did (the fired short-circuit reasons, the winning and
runner-up partitions with the φ-gap, the binding cause/effect direction) —
across IIT 4.0, IIT 3.0, and actual causation, and unified the two divergent
`ShortCircuitConditions` enums into a single `NullResultReason`.
