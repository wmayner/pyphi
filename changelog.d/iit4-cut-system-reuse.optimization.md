The IIT 4.0 system-level partition evaluation now builds the induced cut ``System`` once per partition and reuses it across the cause and effect directions, instead of rebuilding it (and its cut matrix and per-node input graph via ``generate_nodes``) once per direction. The cut depends only on the partition, so this halves the cut-``System`` construction in the inner MIP partition loop with identical Φ and MIP.
