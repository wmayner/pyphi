Stop rejecting system states that are reachable under IIT 4.0. The subsystem-level reachability check held the background's past at its current state, which is the pre-4.0 convention; it now runs only under `background_conditioning="CONDITION_CURRENT_STATE"`. Under the default causal marginalization (Albantakis et al. 2023, Eq. 4) a positive substrate-level probability is sufficient. Candidate systems that were previously skipped by `possible_complexes`, `Substrate.complexes`, and the macro-unit search are now evaluated.
