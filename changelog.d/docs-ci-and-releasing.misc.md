Added a CI job that builds the documentation with executed cells and
warnings as errors (the docs previously never built in CI, which let prose
drift from executed output), and a `RELEASING.md` checklist covering the
verification gates and release mechanics.
