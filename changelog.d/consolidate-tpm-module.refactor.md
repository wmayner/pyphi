Consolidated ``pyphi/tpm.py`` into ``pyphi/core/tpm/``. The legacy ``JointTPM`` class, ``reconstitute_tpm`` helper, and ``simulate`` utility now live in ``pyphi/core/tpm/joint_distribution.py`` alongside their ``JointDistribution`` parent class. The top-level ``pyphi.JointTPM`` export and import path through ``pyphi/core/tpm/__init__.py`` are unchanged. Removed the unused ``ProxyMetaclass`` / ``Wrapper`` dead code that lived only inside the legacy module. The module boundary between ``pyphi/tpm.py`` and ``pyphi/core/tpm/`` introduced during P12b is gone.
