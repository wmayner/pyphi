Regenerated ``test/data/relations/relations_rule110.json`` to match the post-C.1 paper-faithful CES for ``examples.rule110_system()``. Pre-C.1, the cruelest-cut state selection picked the cause ``(0, 0, 0)`` with Φ=0 and zero distinctions, leaving the relations fixture at the empty ``{"relations": []}`` baseline. Under the paper-faithful state cascade the system specifies cause ``(1, 1, 1)`` / effect ``(0, 0, 0)`` with 4 distinctions (three 2-mechanisms over the full purview plus the 3-mechanism) and 11 relations. Relation Φ values cluster at 1.0, 1.06, and 1.33; total relation Φ ≈ 12.25, combining with distinction Φ ≈ 6.25 for a system Φ ≈ 18.49 (matching the regenerated ``test/data/phi_structure/rule110.json``). The companion IIT 3.0 fixture ``ces_rule110.json`` was unaffected and did not need regeneration. Unblocks ``test_relations.py::test_all_relations[rule110]``.
