The MCP server's `plot` tool now accepts `view` and `max_relations` for the
cause-effect-structure plot (`kind="ces"`), so an assistant can request any of
the five `plot_ces` views (for example the 3-D `"hypergraph"`) and plot
analytically-computed structures through the tool rather than dropping to Python.
