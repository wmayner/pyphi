Added a `visualization` reference topic to the MCP server
(`get_iit_reference("visualization")`) covering how to plot a cause-effect
structure: the compute-then-plot path, the five `plot_ces` views, the
`max_relations` rule for analytically-computed relations, plotting a structure
built by hand, and saving figures as self-contained HTML rather than fighting
`kaleido` for a PNG. The "Visualize results" how-to gained matching notes on
saving a figure and on plotting a structure you built yourself.
