Added two perf budget assertions to ``test/test_perf_budget.py`` covering the state-tie code path: (a) an inline AND-XOR ``sia()`` test asserting wall time under 5s on a 2-node substrate (catches blow-ups in the per-state max-min cascade); (b) a no-tie baseline on ``basic_iit4_2026`` asserting <2s wall time (catches constant-cost overhead leaking from the cascade infrastructure into the common path). Goldens deliberately exclude tied-state fixtures (``test/golden/zoo.py:32-33``), so without these the Phase C.1 cascade work would ship without a perf tripwire on its primary exercise path.
