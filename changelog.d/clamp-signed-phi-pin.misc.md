Pinned the clamp / ``signed_phi`` tie-key policy with regression tests. Partitions whose raw integration is non-positive map to clamped ``phi = 0`` via ``|·|+`` (Eqs 19-20). At the default ``mip_tie_resolution`` and ``sia_tie_resolution`` chains, ``signed_phi`` is diagnostic-only and does not drive tie-break: clamped-phi ties surface to higher cascade levels and resolve via canonical lex order. No new strategy keys on ``signed_phi``; the principle of maximal existence treats clamped-zero partitions as not existing at the system level, so a less-negative signed value confers no additional existence.
