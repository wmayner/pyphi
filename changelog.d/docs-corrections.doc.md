Documentation corrections from the review triage: the intrinsic-difference
zero/infinity convention now matches the implementation; partition classes
cite Θ(M,Z) (Albantakis et al. 2023, Eq. 38) and the connection count is
related to the Eq. 23/43 normalization denominators; `Node` marginal axis
semantics state the correct time direction; guided errors and docs advise
config write forms that work; the config-migration error points to the
shipped migration guide; `fig16_substrate`, `fold_perception`, the IIT 3.0
module scheme name, `pyphi.models` class references, IIT 3.0 SIA ordering
semantics, and the actual-causation SIA tie citation (Eq. (A3)) are
corrected; `true_events`/`extrinsic_events` document `Complex` and no longer
discard explicitly passed zero-Φ complexes or empty index tuples.
