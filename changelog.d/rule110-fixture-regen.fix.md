Regenerated ``test/data/phi_structure/rule110.json`` after the paper-faithful state-tie cascade landed. Rule 110's cause direction has two states tied at maximal ``ii_c``: ``(0,0,0)`` and ``(1,1,1)``. The principle of maximal existence (Albantakis et al. 2023 S1 Text) selects the state that maximizes ``φ_s`` at the MIP — ``(1,1,1)`` with ``φ_s = 2.0`` — under which the substrate qualifies as a complex with four distinctions and ``Φ = 18.49``. The old fixture reflected the pyphi-specific cruelest-cut convention that picked ``(0,0,0)`` with ``φ_s = 1.0``, below the complex threshold (``Φ = 0``, zero distinctions).
