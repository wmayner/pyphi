Retired ``pyphi.tpm.backward_tpm`` and ``pyphi.tpm.probability_of_current_state``. Binary cause-TPM computations now flow through the same ``FactoredTPM``-based code path as multi-valued substrates. Two ``phi_structure`` fixtures (``basic_noisy_selfloop.json``, ``grid3.json``) were regenerated; the underlying math is identical (factors agree at machine precision, ~1e-16), but operation-order differences propagate to ~1e-15 drift in ``intrinsic_information.II_c`` — well below the configured precision floor (1e-13). The 23 cross-version regression goldens (``test/test_golden_regression.py``) remain byte-identical.
