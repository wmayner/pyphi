The relation-sum and big-Φ validations in `ces()` now request certified
(`GENERAL`) bound certificates — previously they requested uncertified bounds
and were silent no-ops. When the intrinsic-information requirement binds, the
recorded runner-up partition's φ is now capped with the same state-level
terms, so `explain()`'s φ-gap compares like with like.
