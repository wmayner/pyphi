Widened the P9 loky/cloudpickle flake skip logic in ``test_golden_regression`` to cover all four IIT 3.0 EMD fixtures (``basic_iit3_emd``, ``basic_iit3_emd_tri``, ``basic_subset_iit3_emd``, ``xor_iit3_emd``) and to catch the mechanism_mips-layer manifestation in addition to the SIA-layer one. The flake's symptom — an ``error`` key on a result object that's otherwise expected to be fully populated — can surface at either layer depending on which worker stage hit the BrokenProcessPool. Real numerical regressions still fail; only the documented flake skips.
