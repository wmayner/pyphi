Actual causation analyses (`pyphi.actual`) are unaffected by
`formalism.iit.background_conditioning`: the underlying `System` built for
each `TransitionSystem` is pinned to `"CAUSAL_MARGINALIZATION"`. The AC
background rule is governed separately by
`formalism.actual_causation.background_scheme`.
