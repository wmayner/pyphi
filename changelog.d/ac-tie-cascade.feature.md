Actual causation tie-resolution unified through the cascade primitive (Albantakis et al. 2019, Definition 1 + Exclusion). ``Transition.find_mip`` now routes through ``resolve_ties.resolve_ac_partition_tie``: ``argmin |alpha|`` with a pyphi-canonical ``partition.lex_key()`` fallback for reproducibility. ``Transition.find_causal_link`` routes through ``resolve_ties.resolve_ac_causal_link_tie``: ``argmax alpha`` → minimality filter (no strict-superset purviews) → lex-smallest representative on indeterminate symmetric over-determination. ``CausalLink`` gains a ``purview_ties`` accessor exposing the tied :class:`AcRepertoireIrreducibilityAnalysis` set as a first-class value; ``AcRepertoireIrreducibilityAnalysis`` gains ``partition_ties``. Both round-trip through JSON via the C.7 hash-collision-safe peer-serialization pattern. ``extended_purview`` remains for backward compatibility.
