Added native k-ary cause-TPM computation via per-factor likelihood product. The dispatcher in ``cause_tpm`` routes binary substrates to the legacy SBN-bridge path (preserving byte-identical goldens) and k>2 substrates to the native path. Output for k-ary is the pure joint posterior over past states.
