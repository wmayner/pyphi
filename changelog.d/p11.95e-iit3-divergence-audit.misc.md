Audited the IIT 3.0 code paths that the 2.0 refactor shares with IIT 4.0,
confirming that no 4.0-faithful logic alters IIT 3.0 results, and added
``test/test_iit3_divergence_audit.py`` to lock the result in. An
override-and-diff sweep over the six IIT 3.0 EMD golden fixtures found the
``positive_part`` (``|·|+``) clamp, the ``UnresolvedDistinctions`` wrapping
in ``_compute_distinctions``, and the ``PARTITION_LEX`` mechanism-MIP /
system-SIA tie-breaks to be inert or purely representative (never changing a
φ value), and confirmed the 4.0 cascade resolvers and ``resolve_ties.states``
are never reached on the 3.0 path. The one value-bearing 3.0 tie-break is
``purview_tie_resolution``'s ``PURVIEW_SIZE`` key — the classic "prefer larger
purview" convention, not a 4.0 contaminant; the suite pins that it remains
load-bearing for system φ so a silent regression to PHI-only is caught.
