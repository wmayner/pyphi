Added a Model Context Protocol server (`pip install pyphi[mcp]`, `pyphi-mcp`) that exposes PyPhi to AI assistants — tools to build substrates, run analyses, inspect results, and render PyPhi's built-in visualizations (cause-effect structure, repertoires, connectivity, TPM), plus a bundled, citation-checked IIT reference surfaced as resources, prompts, and a `get_iit_reference` tool.
