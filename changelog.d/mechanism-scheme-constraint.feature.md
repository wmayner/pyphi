Added an eager config constraint pairing `mechanism_partition_scheme` with the
formalism version: IIT 3.0 accepts its bipartition/wedge schemes and now
rejects `JOINT_PARTITION_ALL` at configuration time instead of silently
computing a different quantity than the 2014 paper's φ.
