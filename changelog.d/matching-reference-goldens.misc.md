Added golden regression tests comparing `build_triggered_tpm` and
`triggering_coefficient` against frozen values computed by the original
matching research code (triggered-TPM rows, marginal distributions, and
conditional/marginal probabilities agree exactly; triggering coefficients
agree via the positive-part relation).
